# SPDX-License-Identifier: (GPL-2.0-only OR BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/input/touchscreen/novatek,nt36675-spi.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: Novatek NT36xxx series touchscreen controller

maintainers:
  - George Chan <gchan9527@gmail.com>

allOf:
  - $ref: touchscreen.yaml#

properties:
  compatible:
    oneOf:
      - description: Implements extra matching in bootargs for keywords like "tianma" or "nt36"
        items:
          - enum:
            - novatek,nt36525-spi
            - novatek,nt36672a-spi
            - novatek,nt36675-spi
            - novatek,nt36676f-spi
            - novatek,nt36772-spi
          - const: novatek,NVT-default-spi

      - description: Implements simple compatible string to nt36xxx IC
        items:
          - enum:
            - novatek,nt36525-spi
            - novatek,nt36672a-spi
            - novatek,nt36675-spi
            - novatek,nt36676f-spi
            - novatek,nt36772-spi

      - description: Implements simple compatible string to probe only nt36xxx IC and fail.
        items:
          - enum:
            - novatek,NVT-default-spi

  reg:
    maxItems: 1

  irq-gpio:
    maxItems: 1

  vdd-supply:
    description: Power supply regulator for VDD pin

  vio-supply:
    description: Power supply regulator on VDD-IO pin

  firmware-name:
    description: Firmware for device init, if unspecify, all
                  treat as no firmware needed except nt36675, default
                  to "novatek_ts_tianma_fw.bin".

  spi-max-frequency:
    description: Set max frequency to spi bus communication. This is optional.

unevaluatedProperties: false

required:
  - compatible
  - reg
  - irq-gpio

examples:
  - |
    #include <dt-bindings/interrupt-controller/irq.h>
    #include <dt-bindings/gpio/gpio.h>

    spi {
      #address-cells = <1>;
      #size-cells = <0>;

      touchscreen@0 {
        compatible = "novatek,nt36675-spi";
        reg = <0>;
        spi-max-frequency = <4000000>;
        irq-gpio = <&tlmm 9 0x2001>;
        firmware-name = "novatek_ts_tianma_fw.bin";
      };
    };

...
